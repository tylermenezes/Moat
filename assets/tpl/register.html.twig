{% extends "template.html.twig" %}
{% block active %}register{% endblock %}
{% block content %}
    {% if username_taken %}
        <section class="warning">
            Bummer. That username was already taken.
        </section>
    {% elseif missing_fields %}
        <section class="warning">
            All fields are required.
        </section>
    {% elseif password_mismatch %}
        <section class="warning">
            Oops! Your passwords didn't match.
        </section>
    {% elseif invalid_phone %}
        <section class="warning">
            Something looks weird with your phone number...
        </section>
    {% elseif invalid_email %}
        <section class="warning">
            Something looks weird with your email...
        </section>
    {% endif %}

    <section class="content register">
        <header>
            <h1>We're excited to see you're here. Let's get started.</h1>
        </header>

        <form method="post">
            <h2>Login</h2>
            <p>How can we authenticate you?</p>
            <fieldset class="login">
                <input type="text" name="username" value="{{ username }}" placeholder="Username" required />
                <input type="password" name="password" value="" placeholder="Password" required />
                <input type="password" name="password_confirm" value="" placeholder="Password, once more" required />
            </fieldset>

            <h2>Your Name</h2>
            <p>We have such a bad memory for names; what was yours, again?</p>
            <fieldset class="name">
                <input type="text" name="first_name" value="{{ first_name }}" placeholder="First Name" required />
                <input type="text" name="last_name" value="{{ last_name }}" placeholder="Last Name" required />
            </fieldset>

            <h2>Contact</h2>
            <p>This information will only be available internally, never publicly.</p>
            <fieldset class="contact">
                <input type="text" name="phone" value="{{ phone }}" placeholder="(555) 555-5555" required />
                <input type="text" name="email" value="{{ email }}" placeholder="me@me.com" required />
            </fieldset>

            <input type="submit" value="Engage!" />
        </form>
    </section>
{% endblock %}
